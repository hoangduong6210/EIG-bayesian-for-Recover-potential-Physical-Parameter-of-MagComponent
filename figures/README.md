# Figures

`generated/` contains publication figures produced from the release selected by
`paper/current_state/results.lock.yaml`. `source/` documents figure-source
conventions.

Every data-derived figure records its release ID and input artifact hashes in a
sidecar manifest.
