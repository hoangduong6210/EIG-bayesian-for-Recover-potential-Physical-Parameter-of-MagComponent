# Processed data

This directory is reserved for run-derived data. Contents are reproducible, disposable, and never treated as source evidence. Production runs should use task-unique directories outside the tracked tree.

